export default class StyleModel {
    private type: string = 'DEFAULT'; // по-умолчанию будет стиль берется из vuelayers default style
    private strokeColorValue: string = '#00aaff';
    private backgroundColorValue: string = 'rgba(255,255,255,0.8)';
    private strokeWidth: number = 3;
}
